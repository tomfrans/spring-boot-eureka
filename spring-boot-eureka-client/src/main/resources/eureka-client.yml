# Spring properties
spring:
  application:
     name: eureka-client

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
 # instance:
  #  prefer-ip-address: false    
#    non-secure-port: 80

# HTTP Server
server:
  port: 8001   # HTTP (Tomcat) port